# 期货多周期 “看大做小” 回测框架

> 从 0 出发回答一个问题：**在期货市场里，“看大周期做小周期” 能不能帮我们做到更高、更稳的年化？**
> 本目录在原股票回测框架上扩展出一套商品期货 **多周期（multi-timeframe）** 子系统，
> 用国内 36 个主力连续合约、最长 21 年的日线数据做了系统回测。

## 1. 思路：看大做小（Top-Down Multi-Timeframe）

主流做法（多周期共振 / 看大做小）的内核是一句话：

> **大周期定方向，小周期找入场，只做与大周期同向的单。**

- **大周期（HTF）**：把日线重采样成 **周线**，用 EMA(8/20) 排列 + 斜率判定趋势方向
  `bias ∈ {-1, 0, +1}`（多 / 震荡观望 / 空）。
- **小周期（LTF）**：在 **日线** 上用均线交叉 / 唐奇安突破 / MACD 择时。
- **共振过滤**：最终信号 = 小周期入场 **且方向与大周期一致**，否则空仓。多空双向。

所有信号都做了 **防未来函数** 处理（大周期取已完成的上一根 K 线，信号 T 根收盘定、T+1 生效）。

## 2. 代码结构

| 文件 | 作用 |
|---|---|
| `src/data/futures.py` | 新浪期货主力连续合约数据源（日线/分钟，带本地缓存）+ 多周期重采样；按 5 大板块组织品种 |
| `src/backtest/futures_engine.py` | **收益率型** 期货回测器：多空双向、目标波动率定仓/杠杆、换月跳空截断、组合合成 |
| `src/strategies/mtf.py` | 看大做小信号：`htf_bias` / `ltf_signal` / `mtf_signal`（支持 mtf / ltf_only / htf_only 对照） |
| `scripts/run_futures_mtf.py` | 主跑批：分品种/分板块/全市场组合 + 入场速度扫描，输出 CSV/图/报告 |
| `scripts/run_intraday_mtf.py` | 日内演示：日线定方向 + 60 分钟择时（样本约 9 个月，仅演示） |

与股票引擎的关键区别：商品期货 **带杠杆、可做空**，所以用 **目标波动率定仓**
（每个品种缩放到 15% 年化波动、最高 3x 杠杆），让不同品种年化可比、杠杆显性化；
用 **连续合约收盘价的百分比收益** 计算损益，并对换月跳空（单日 |收益|>15%）做截断。

## 3. 怎么跑

```bash
pip install pandas numpy requests matplotlib pyarrow
python download_data.py   # 股票（可选）

# 一键下载期货数据并回测（首次自动联网拉取并缓存到 data/futures/）
PYTHONPATH=. python scripts/run_futures_mtf.py
PYTHONPATH=. python scripts/run_futures_mtf.py --start 2015-01-01   # 指定区间
PYTHONPATH=. python scripts/run_futures_mtf.py --category 黑色       # 单板块

# 日内演示
PYTHONPATH=. python scripts/run_intraday_mtf.py
```

输出在 `results/`：`REPORT.md`、`summary_by_*.csv`、`entry_speed_sweep.csv`、`equity_curves.png`。

## 4. 主要结论（2005~2026，全市场 36 品种，目标波动 15%）

### 4.1 “看大做小” 的价值集中在『快入场』

组合层面对不同日线快慢做 mtf vs 仅日线（ltf_only）对比：

| 日线入场 | 仅日线夏普 | +看大做小夏普 | 结论 |
|---|---|---|---|
| 快 ma3/10 | 0.38 | **0.54** | 大周期过滤明显救活快择时（回撤也更小） |
| 中 ma5/20 | 0.68 | 0.66 | 基本持平 |
| 慢 ma10/30 | 0.69 | 0.57 | 慢择时本就平滑，过滤反而削减暴露 |

> **越快的小周期越容易被假信号打损，叠加大周期趋势过滤后夏普/回撤显著改善；
> 越慢则增量越小甚至拖累。** 这正是看大做小的教科书逻辑，在 21 年数据上得到验证。

### 4.2 板块底层逻辑不同，顺势有效性差异巨大

| 板块 | 组合夏普 | 说明 |
|---|---|---|
| 能化 | ~0.72 | 工业品，产业链 + 油价驱动，趋势性强 |
| 黑色 | ~0.56 | 工业品，地产/基建周期驱动 |
| 贵金属 | ~0.51 | 宏观/货币属性，**看大做小增量最大**（均值夏普 0.42 vs 仅日线 0.15） |
| 有色 | ~0.39 | 全球经济周期 + 金融属性 |
| **农产品** | **~ -0.14** | 天气/收储等供给端随机冲击，趋势性弱、易反复，顺势最差 |

> 工业品/贵金属趋势性强、适合顺势 + 看大做小；农产品需要不同逻辑（基本面/季节性/
> 套利），单纯顺势会亏钱。**“分品种用不同逻辑” 是对的。**

### 4.3 真正把年化做高、做稳的杠杆

全市场等权、按 15% 目标波动加杠杆的 CTA 组合：**年化 ~9%、夏普 ~0.66、最大回撤 ~37%、21 年**。
- “看大做小” 不是万能收益放大器，而是 **风险/择时过滤器**。
- 稳健年化的真正来源是 **多品种分散 + 波动率目标定仓 + 板块筛选（剔除/区别对待农产品）**，
  再用 “看大做小” 给快择时上一道保险。把目标波动调高（加杠杆）即可等比放大年化（夏普不变）。

## 5. 局限与后续

- 主力连续合约由新浪直接拼接（未后复权），换月有跳空；已用收益截断缓解，但严谨研究应使用
  **后复权连续合约 / 真实换月规则**。
- 分钟数据仅最近约 1000 根，日内结论仅作框架演示，不可当长期年化。
- 参数（趋势窗口、突破周期、目标波动）未做严格样本外检验，存在一定过拟合风险；
  可进一步做 **滚动样本外、分板块差异化参数、加 ATR 止损/移动止盈、品种间风险预算**。
