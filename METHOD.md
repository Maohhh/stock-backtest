# 期货求魔交易系统 — 方法论笔记

来源：bilibili UP 主「期货求魔」9 集教学视频（5 分钟波段 + 日内交易篇）。
原视频和逐字稿见 `videos/` 与 `transcripts/`。本文是把零散讲解整理成可代码化的规则集，便于参数化与回测。

## 零、作者声明的胜率（参考，未独立验证）

作者在视频 5_02 自述："严格执行下，最近一年左右的统计胜率约 80%。"该数字来自作者口述，没有公开数据集复现。本仓库代码不保证任何回测结果接近此口径——把它当作设计目标，不当作业绩承诺。

## 一、核心抽象

在一张图上叠三条 SMA，把它们当作不同强度的"支撑/压力骨架"：

| 均线 | 角色 | 用途 |
| --- | --- | --- |
| MA20 | 快线 / 出场参考 | 日内出场触发；3 分钟模型主交易线 |
| MA120 | 中线 / 部分止盈 | 5 分钟模型的过滤线和部分止盈触发 |
| MA250 | 慢线 / 趋势锚 | 多空分界、波段入场点、最后止损线 |

行情属于三种状态之一，由 MA250 的斜率判定：

| MA250 斜率 | 状态 | 操作 |
| --- | --- | --- |
| 显著向上 | 多头趋势 | 只做多 |
| 显著向下 | 空头趋势 | 只做空 |
| 接近水平 | 震荡 | 观望（最重要的纪律） |

在代码里用 `slope_slow` 列度量斜率（最近 N 根 K 线的线性回归斜率，除以价格归一化）。`slope_eps` 是触发"趋势"的最小阈值，默认设为序列绝对斜率的中位数；想更严格就调大。

## 二、5 分钟波段模型（视频 5_01 – 5_05）

### 入场（做多；做空镜像）

两类信号都要求"价格回踩到 MA250 附近"。代码列名（`signal1_long` / `signal2_long`）与讲解里的"信号 1 / 信号 2"**不完全等同**——讲解侧重"理想型 vs 洗盘型"，代码列按图形特征命名：

- **`signal1_long`（W 底/双底，对应讲解里的"信号 2 洗盘型"）**
  连续两个 swing low 都靠近 MA250，且第二个低点 ≥ 第一个；价格突破两个低点之间的最高点 → 触发。讲解：第一次破 MA250 看似假突破，回来再次突破时新的 swing low 出现 → 入场。
- **`signal2_long`（抬高底回踩，对应讲解里的"信号 1 理想型"的弱版本）**
  价格回踩到 MA250 后，在 MA250 上方打出一个更高的 swing low；之后收盘重新站上 MA250（且斜率确认上行）→ 触发。讲解里的"理想型"还要求回踩**没有跌破** MA250；本代码不强制这一条以避免错过临界回踩。

讲解 5_03 还提到第三类（**激进入场**）：经验老到的交易者会在 swing low 回踩到 MA250 的那一根 K 线就直接介入，不等突破确认；代价是更高的假信号率，好处是开仓成本更低、止损更紧。本仓库未默认开启；要用的话直接以 `feat["near_slow_low"] & (regime==1)` 作为触发即可。

代码里的"靠近"用 `near_tol` 控制（默认 0.3%，5 分钟期货的合理近似）；"swing low"用 `pivot_k`（默认 3）控制——左右各 3 根 K 线内的最低点。震荡区**斜率即将变陡**的预判可以看 `mid_minus_slow` 列（MA120 - MA250 归一化价差）：当该值绝对值快速放大时，MA250 即将转向同方向陡化。

### 出场（持仓阶段的移动止盈，作者强调"会平仓才是高手"）

按触发先后给一个三档梯子（多头版；空头镜像）：

1. **极端价差（少见但优先）**
   `|close - MA250|` 在最近 `extreme_window` 根 K 线里的百分位 ≥ `extreme_pct`（默认 95%） → 全部止盈。
   原讲解说"近 1 年最大价差"，对应 5 分钟约 12000 根；默认 240 是个轻量近似，按品种自行调大。
2. **MA120 跌破（常见预警）**
   收盘跌破 MA120 → 减仓大部分，留少量底仓（这是讲解里的"留小部分底仓"）。
   `run_backtest` 是向量化的不细分部分仓位，默认把这一档视作全平；若想严格分级，自行扩展。
3. **MA250 跌破（最后底线）**
   收盘跌破 MA250 → 全部止盈/止损。MA250 是"必须严格执行"的那根线，作者反复强调。

代码里 `exit_long_loose` = 收 < MA120，`exit_long_tight` = 收 < MA250，`spread_extreme_long` = 价差极端 + 持多。

### 震荡处理

MA250 斜率落在 `[-slope_eps, +slope_eps]` 区间 → 观望，本系统不主动开仓。
原讲解："根据 250 日均线的斜率来判断是否震荡市，从而回避"。

5_02 强调一个现实：**震荡通常事后才知道**。最常见的识别方式是吃了 1-2 次止损或保本之后才反应过来当前是震荡。这意味着初始阶段的几次"假信号"是系统不可避免的成本，靠 `slope_eps` 调大可以减少触发但也会错过弱趋势的真入场。

### 偏离度的双重用途（5_02 + 04）

`spread_extreme_long` 与 `spread_extreme_short` 这两列在不同状态下意义相反：

- **开仓阶段（空仓）**：极端偏离 = 逆势进场候选。`spread_extreme_long`（价格远在 MA250 上方）→ 考虑做空；`spread_extreme_short` → 考虑做多。
  作者强调：这是逆势操作，必须配严格止损，仅在快速极端波动时考虑，不建议常规使用。本仓库的 `QiuMoSwingStrategy` 默认不启用该逆势入场（避免和趋势跟随逻辑混淆）。
- **持仓阶段（已有仓位）**：同向极端偏离 = 全部止盈。`QiuMoSwingStrategy` 默认使用这种解读：持多遇 `spread_extreme_long` → 平仓。

如果你要把逆势入场也加入策略，可以扩展 `QiuMoSwingStrategy` 把 `spread_extreme_short` 转为 `entry_long_reversal`、`spread_extreme_long` 转为 `entry_short_reversal`，并自带独立的较紧的止损参数。

## 三、日内交易模型（视频 01 – 04）

### 基本原则（01）

- 不持仓过夜、不持仓过午后收盘到夜盘开盘的窗口；午盘是否持仓看品种与外盘风险。
- 偏低频，每天数次到十几次，超过 20 次基本是行情把人逼出来的；高频/炒单要看天赋，不推荐普通人。
- 收益可大可小，亏损只能小（盈亏不对称地砍亏控盈）。
- 最大风险是"做单情绪上头后把日内拖成隔夜"，纪律比信号重要。

### 3 分钟模型（02）

- 主交易线：MA20。线上做多、线下做空；价格有效上破/下穿 MA20 = 止损或止盈。
- 3 分钟的 MA250 同样是该周期的多空分界：在 MA250 下方沿 MA20 上方做多，最好保证 MA250 和 MA20 间还有"空间"，反之亦然。
- 注意：3 分钟波动碎、对反应速度敏感，作者本人定位为偏高频；新手优先看 5 分钟模型。

### 5 分钟日内模型（03）

- 入场依据：MA120 和 MA250。当 MA120/MA250 形成支撑/压力且 MA250 给出方向时入场。
- 出场依据：MA20。收盘穿越 MA20 即平仓。
- 实质：把波段模型的"入场要严格、出场要果断"压到一天之内。

### 偏离度反转入场（04）— 短周期均线领先长周期反转

这是一个独立的反转入场触发器，原 04 集专门讲：

1. **判定偏离过大**：5 分钟 K 线相对 MA250 偏离已经持续一段时间（讲解口径：1 周 / 半月 / 1 个月，对应 5min ≈ 1200 / 2400 / 4800 根）。代码里 `spread_extreme_long/short` 已经表达这个状态。
2. **小周期均线先反转**：先看 MA20。下跌一段后收盘**重新站上 MA20**，且 MA20 角度逐步走平→上拐。
3. **等回踩确认**：上破 MA20 不要直接追，等价格回踩 MA20 不破，再开多。镜像做空。
4. **持仓回踩阶梯**：开仓后沿 MA20 持有；遇 MA120 回踩不破继续持有；遇 MA250 回踩不破继续持有；任一档**有效跌破**触发对应级别的减/平仓。
5. **关键句**："小周期均线往往是长周期均线反转的先行指标。"

代码层面，反转入场可以这样组合现有列：

```python
# 多头反转候选 = 最近 N 根内出现过 spread_extreme_short
# 现在收盘上穿 MA20 且 MA20 向上
import numpy as np
recent_overshot = feat["spread_extreme_short"].rolling(120, min_periods=1).max().astype(bool)
crossed_up_fast = (df["close"].shift(1) <= feat["ma_fast"].shift(1)) & (df["close"] > feat["ma_fast"])
ma_fast_up = feat["ma_fast"].diff(3) > 0
reversal_long = recent_overshot & crossed_up_fast & ma_fast_up
```

这种触发器不在 `QiuMoSwingStrategy` 默认行为里（会显著改变交易性质），需要时自行包装成一个 `QiuMoReversalStrategy` 或在你自己的策略里 OR 进去。

## 四、风控与执行（5_05）

非技术性内容，但作者花了一整集讲。摘要：

- 风险意识只能靠：实战亏损、复盘失败案例、和专业人士交流。
- 提高执行力的被动手段：云条件单、资金管理（鸡蛋别一篮子）、仓位管理。
- 主动手段：复盘和交易计划、归纳高胜率信号、参加模拟考核磨练规则执行。
- 关键句："会开仓只是入门，会平仓才是高手。"

## 五、参数默认值与调参建议

| 参数 | 默认 | 含义 / 调参方向 |
| --- | --- | --- |
| `fast / mid / slow` | 20 / 120 / 250 | 讲解定义，三条 SMA。可整体缩放（如 5/20/120）模拟 3 分钟模型 |
| `slope_window` | 10 | MA250 斜率拟合窗口（根） |
| `slope_eps` | None（自适应=绝对斜率中位数） | 越大越严格。固定品种时建议先看分布再设 |
| `pivot_k` | 3 | swing 检测半宽。商品波动大的品种调大到 5 |
| `near_tol` | 0.003 | 视为靠近 MA250 的相对距离。豆粕/沪铜 0.002，原油/股指期货 0.005+ |
| `extreme_window` | 1200 | 价差极端窗口（≈ 5min 周期下的 1 周）。讲解口径：1 周 / 半月 / 1 月 / 1 年 → 5min 对应 1200 / 2400 / 4800 / 12000 |
| `extreme_pct` | 0.95 | 价差百分位阈值；想更少触发设 0.99 |
| `use_partial_exit` | True | 是否启用 MA120 跌破的部分平仓 |
| `max_hold_bars` | None | 最长持仓上限（5min K 线根数）。日内策略可设 48（4 小时） |

## 六、本实现已知近似与局限

- **Pivot 居中检测有 `pivot_k` 根右侧前瞻**。实战可见的 swing low 要在右侧 `pivot_k` 根 K 线后才能确认；回测里这部分前瞻最多偏置约 15 分钟（5 分钟 K 线 × 3），对 5 分钟波段策略影响小，但严格无前瞻请把信号整体右移 `pivot_k` 根。
- **部分平仓被简化为全平**。`run_backtest` 是向量化的二值仓位；要严格还原"MA120 跌破留底仓"，请自己包一个事件驱动版。
- **极端价差窗口口径**。默认 1200 根（≈ 5min 周期下的 1 周）已对齐讲解里的最短口径；要严格还原"近 1 年最大价差"请设 12000 根，但短回测数据要够长。
- **日内策略未实现尾盘强平**。`QiuMoDayStrategy.generate_signals` 接受 `session_close` 布尔列，由调用方根据交易所收盘时间标注。
- **未对冲跳空和涨停**。期货跨日有跳空，5 分钟 K 线策略需要在跨日 bar 做额外处理；本实现按连续序列处理。

## 七、参考

- 视频列表：`urls.txt` / 原始 mp4：`videos/`
- 逐字稿：`transcripts/`
- 指标实现：`src/indicators/qiumo.py`
- 策略实现：`src/strategies/qiumo_strategy.py`
- 端到端示例：`qiumo_demo.py`
