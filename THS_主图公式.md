# 同花顺主图指标公式（单标的趋势集成）

这是把 `src/indicators/trend_ensemble.py` 的单标的趋势指标，翻译成可直接粘贴进
**同花顺（通达信兼容语法）指标公式编辑器**的主图公式。因为是单品种指标，它能真正上主图
（横截面策略做不到，见 FUTURES_RESULTS.md 第 0 节）。

> 推荐用在**宏观驱动、趋势性强**的品种上：黄金 AU（最优）、沪铜 CU、棉花 CF、沪铝 AL。
> 震荡品种（多数化工/黑色）上趋势策略接近 0，不建议。

## 公式代码

```
{===== 多周期趋势集成 + 效率系数过滤（单标的主图）=====}

{1) 多周期 EMA 快慢线投票}
V:=(IF(EMA(C,8)>EMA(C,32),1,-1)
   +IF(EMA(C,16)>EMA(C,64),1,-1)
   +IF(EMA(C,32)>EMA(C,128),1,-1))/3;

{2) Kaufman 效率系数 ER(20)：净位移/路程，越接近1越趋势}
NET:=ABS(C-REF(C,20));
PATH:=SUM(ABS(C-REF(C,1)),20);
ER:=NET/PATH;
ERTH:=0.3;                 {效率阈值，低于此=震荡=空仓}

{3) 趋势状态}
LONG:=V>0 AND ER>ERTH;     {多头趋势}
SHORT:=V<0 AND ER>ERTH;    {空头趋势}

{4) 主图绘制}
慢线:EMA(C,128),COLORGRAY;
DRAWTEXT(LONG AND REF(LONG,1)<>1,L*0.985,'多'),COLORRED;     {转多标记}
DRAWTEXT(SHORT AND REF(SHORT,1)<>1,H*1.015,'空'),COLORGREEN; {转空标记}
STICKLINE(LONG,H,L,0,1),COLORRED;       {多头K线染红}
STICKLINE(SHORT,H,L,0,1),COLORGREEN;    {空头K线染绿}
```

## 信号含义

- **“多”箭头**：三组均线多数转多 **且** 处于趋势态（ER>0.3）→ 做多/持多。
- **“空”箭头**：多数转空且趋势态 → 做空/持空。
- **无标记（K线本色）**：ER≤0.3 震荡区，**空仓观望**（约 70% 的时间）。

## 仓位管理（公式只给方向，手数需自己按波动率定）

回测里的"不爆仓"靠**波动率目标**：手数 = 目标风险 / 近 20 日波动。同花顺主图公式不便算手数，
建议按下式手动/在交易端设定（与回测一致）：

```
单品种下单手数 ≈ (账户资金 × 目标年波动 / √252) / (合约价值 × 近20日日波动率)
```

并设：单品种杠杆上限 ≤ 3x、总回撤熔断（如 -20% 暂停）。

## 回测基准（黄金 AU，日线，单边 2bp）

| 区间 | 夏普 | 年化(目标波动30%) | 最大回撤 |
|---|---|---|---|
| 训练 ≤2018 | 0.16 | 2.7% | -24% |
| **样本外 2019–2026** | **0.86** | **18%** | -27% |

年化-杠杆前沿（样本外）：20%波动→13%/−19%；30%→20%/−27%；40%→26%/−36%；60%→36%/−49%。

⚠️ 诚实提示：样本外强劲主要来自 **2024–2026 黄金大牛市**；震荡年份（2019–2023）基本走平不亏。
这是趋势策略的固有特征——**趋势赚大钱、震荡靠空仓不亏**，而非平滑高夏普。单标的方向性指标
的真实天花板就在夏普≈0.9 一带，想要更高年化更低回撤，需要多品种横截面组合（见 `run_combined_book.py`）。

---

# 2. 农产品配对价差回归（玉米-淀粉）副图公式

价差 Z 分数是一条振荡线，适合做**副图指标**。同花顺单图只有一个品种，需用跨品种引用
取另一条腿的收盘价。下式以**玉米淀粉(CS)主连**为当前图、引用**玉米(C)主连**：
（不同版本跨品种函数名可能不同，常见为 `"代码$CLOSE"` 或 `CODE("...")`；
若你的版本不支持，可在交易端用我们的 Python `run_ag_spread.py` 实时算 Z 值。）

```
{===== 玉米-淀粉 价差Z回归（副图）=====}
N:=30;
C2:="大商所玉米主连$CLOSE";      {引用另一腿玉米收盘(玉米/淀粉均在大商所), 视平台改写代码}
SP:=CLOSE-C2;                    {价差 = 淀粉 - 玉米}
Z:(SP-MA(SP,N))/STD(SP,N),COLORYELLOW;   {Z分数主线}
{阈值线}
上轨:1.5,COLORRED;  下轨:-1.5,COLORGREEN;
止盈上:0.3,COLORGRAY; 止盈下:-0.3,COLORGRAY;
止损上:4,COLORWHITE; 止损下:-4,COLORWHITE;
{信号}
DRAWTEXT(CROSS(Z,1.5),Z,'空价差'),COLORRED;
DRAWTEXT(CROSS(-1.5,Z),Z,'多价差'),COLORGREEN;
```

交易规则：Z 上穿 +1.5 → 做空价差（卖淀粉买玉米）；Z 下穿 −1.5 → 做多价差；
Z 回到 ±0.3 内止盈；Z 超 ±4 止损。胜率约 68%、盈亏比约 1.06、盈利因子约 2.5。
详见 [`AG_SPREAD_RESULTS.md`](AG_SPREAD_RESULTS.md)。

---

# 3. 跨品种套利价差 Z 回归（主图，直接挂在套利标的上）★

**这是最理想的用法**：券商挂牌的"跨品种套利标的"本身是单一标的，
它的 K 线收盘价 `CLOSE` 就是**价差**。所以直接对这条 K 线自己做 Z 回归即可——
纯单标的、可直接做主图指标。把套利标的（如玉米淀粉套利、螺纹热卷套利）的图拉出来，套用本指标。

```
{===== 跨品种套利 价差Z回归（主图，挂在套利标的上）=====}
N:=30;
MID:MA(CLOSE,N),COLORWHITE;              {价差均值}
SD:=STD(CLOSE,N);

{开仓 / 止盈 / 止损 价格带（画在价差K线上）}
开空线:MID+2.0*SD,COLORRED;             {价差升破此线 -> 做空价差}
开多线:MID-2.0*SD,COLORGREEN;           {价差跌破此线 -> 做多价差}
止盈上:MID+0.5*SD,COLORGRAY;
止盈下:MID-0.5*SD,COLORGRAY;
止损上:MID+4*SD,COLOR888888;
止损下:MID-4*SD,COLOR888888;

{信号箭头}
DRAWTEXT(CROSS(CLOSE,MID+2.0*SD),CLOSE,'空价差'),COLORRED;     {升破+2σ:做空价差}
DRAWTEXT(CROSS(MID-2.0*SD,CLOSE),CLOSE,'多价差'),COLORGREEN;   {跌破-2σ:做多价差}
DRAWTEXT(CROSS(MID+0.5*SD,CLOSE),MID,'平空'),COLORGRAY;        {回到+0.5σ:平空止盈}
DRAWTEXT(CROSS(CLOSE,MID-0.5*SD),MID,'平多'),COLORGRAY;        {回到-0.5σ:平多止盈}

{趋势预警: 价差处于单边趋势时(效率系数ER>0.5)染黄背景, 此时少抄底/摸顶}
ER:=ABS(CLOSE-REF(CLOSE,10))/SUM(ABS(CLOSE-REF(CLOSE,1)),10);
BACKSET(ER>0.5,0);
STICKLINE(ER>0.5,MID+4*SD,MID-4*SD,8,1),COLOR222200;
```

**看图操作**：
- 价差（K线）**升破红线(+2σ)** → 做空价差（卖贵的腿、买便宜的腿）；
- 价差**跌破绿线** → 做多价差；
- 价差**回到灰色带(±0.5σ)内** → 止盈平仓；
- 价差**冲破灰色止损线(±4σ)** → 止损离场。
- **黄色背景 = 价差在单边趋势中**（效率系数 ER>0.5）：此时回归信号可靠性低，
  **少抄底/摸顶**，等价差走稳（黄背景消失）再做。这是为了解决"趋势行情里逆势开仓被套"的问题。

**副图纯 Z 线版本**（想直接看 Z 数值）：

```
N:=30;
Z:(CLOSE-MA(CLOSE,N))/STD(CLOSE,N),COLORYELLOW;
A:2.0,COLORRED;  B:-2.0,COLORGREEN;  C:0.5,COLORGRAY; D:-0.5,COLORGRAY;
E:4,COLOR888888; F:-4,COLOR888888;
```

**适用的 3 个最终套利标的**（同一公式套在每个标的上即可）：
**玉米淀粉套利（C&CS）、PVC聚丙烯套利（V&PP）、棉花棉纱套利（CF&CY）**——
三者都是体量相当的 1:1 标的，与公式完全匹配。

⚠️ **三个注意点**
1. **确认价差方向**：挂牌套利标的的报价是"腿A−腿B"还是"腿B−腿A"，看一眼即可对应多空方向（做反了把买卖对调）。
2. **只用 1:1 体量相当的对**：上面 3 个都是 1:1 干净对。豆油-棕榈、豆一-豆粕等**正确对冲比例不是 1:1**，
   按挂牌 1:1 标的交易并不赚钱，已剔除——别用本公式去挂那些标的。
3. **周期**：回测用日线，建议挂日线图；参数 N=30、±2.0/±0.5/±4 已是 Z 标准化、与品种无关，3 个标的通用。
